@model IEnumerable<GYMWebApp.DAL.HareketGrubu>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Main.cshtml";
}


<p>
    <h3>@Html.ActionLink("Yeni Hareket Bölgesi Ekle", "Create")</h3>
</p>
<table class="table">
    <tr>
        <th>
            HAREKET ADI
            @*@Html.DisplayNameFor(model => model.HareketAdi)*@
        </th>
        <th>İŞLEMLER</th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.HareketAdi)
        </td>
        <td>
            <button type="button" class="bg-yellow">  @Html.ActionLink("Düzenle", "Update", new { id = item.Id }) </button>  
            <button type="button" class="bg-red">@Html.ActionLink("Sil", "Delete", new { id = item.Id }) </button>
        </td>
    </tr>
}

</table>
